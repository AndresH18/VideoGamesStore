@model Game

<div class="card m-1 p-1">
    <div class="bg-gradient p-1">
        <h4>
            @Model.Name
            <span class="badge round-pill bg-primary text-white" style="float:right">
                <small>@Model.Price.ToString("C")</small>
            </span>

        </h4>
    </div>
    @* TODO: replace div with form, add to cart*@
    <div>
        <span class="card-text p-1">
            <button class="btn btn-success btn-sm" style="float:right">Add to Cart</button>
        </span>
    </div>
</div>